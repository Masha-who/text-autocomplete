Text Autocomplete Project

Проект посвящён задаче автодополнения текста с использованием рекуррентной нейронной сети (LSTM), а также предобученного трансформера (distilgpt2).

Модель обучается предсказывать следующую часть текста по входной последовательности. Качество оценивается с помощью метрики ROUGE по сценарию:
модель получает первые 3/4 текста, необходимо дополнить оставшуюся 1/4. 

📁 Структура проекта
text-autocomplete/
├── data/                            # Датасеты
│   ├── raw_dataset.zip              # Скачанный "сырой" датасет
│   └── dataset_processed.zip        # Очищенный датасет
│
├── src/                             # Исходный код проекта
│   ├── data_utils.py                # Очистка и предобработка текста
│   ├── next_token_dataset.py        # Реализация torch.Dataset
│   ├── lstm_model.py                # LSTM-модель для автодополнения
│   ├── eval_lstm.py                 # Замер ROUGE для LSTM
│   ├── lstm_train.py                # Обучение LSTM
│   ├── eval_transformer_pipeline.py # Оценка distilgpt2
│
├── configs/                         # YAML-конфиги (гиперпараметры)
│
├── models/                          # Сохранённые веса моделей
│
├── solution.ipynb                   # Ноутбук с демонстрацией решения
└── requirements.txt                 # Зависимости проекта


Этап 1. Подготовка данных

- Очистка текста: приведение к нижнему регистру, удаление ссылок, упоминаний, лишних символов
- Токенизация
- Добавление специального токена конца последовательности
- Разбиение на train, validation, test
- Создание torch.Dataset и DataLoader

Файл: src/data_utils.py
Dataset: src/next_token_dataset.py

Этап 2. LSTM-модель

Имплементация модели на основе nn.LSTM.

forward() — используется при обучении (предсказание следующего токена)
generate() — используется для автодополнения текста

Файл: src/lstm_model.py

Этап 3. Обучение LSTM

Во время обучения выводятся:

Train Loss
Validation Loss

ROUGE-1
ROUGE-2
ROUGE-L

Метрика считается по сценарию: 
- модель получает 3/4 текста и дополняет оставшуюся 1/4

Обучение: src/lstm_train.py
Метрики: src/eval_lstm.py

Этап 4. Использование трансформера (distilgpt2)

Используется модель distilgpt2

Для генерации текста применяется transformers.pipeline.

Также рассчитывается ROUGE по аналогичному сценарию (модель получает 3/4 текста и дополняет оставшуюся 1/4).

Файл: src/eval_transformer_pipeline.py

Этап 5. Сравнение моделей

Сначала сравниваются LSTM модели со следующими параметрами:
- hidden_dim=128, batch=256, lr=1e-3, без dropout 
- hidden_dim=128, batch=256, lr=5e-4, dropout=0.3 - сравним, будет ли обучаться стабильнее
- hidden_dim=256, batch=256, lr=1e-3, без dropout - посмотрим, будут ли улучшения в связи с увеличением сложности модели
- hidden_dim=256, batch=256, lr=5e-4, dropout=0.3 - добавим дропаут, понаблюдаем за стабильностью обучения

Затем сравниваются:
- LSTM (выбранная из 4 вариантов выше модель)
- distilgpt2 (предобученный трансформер)

Сравнение выполняется по метрике ROUGE и качеству примеров автодополнения.


📊 Ожидаемые результаты

LSTM показывает базовое качество автодополнения. Конечно, distilgpt2 превосходит LSTM по ROUGE
Однако, если необходима легковесная модель - возможно использование LSTM.


👤 Автор

Проект выполнен в рамках задания по NLP.